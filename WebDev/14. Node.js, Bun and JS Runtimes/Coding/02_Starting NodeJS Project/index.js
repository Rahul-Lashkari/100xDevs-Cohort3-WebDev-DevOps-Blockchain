/**
 * You can run this code by using the following command
 *   $ node index.js
 *   $ bun index.js
 *   $ npm run start
 *   $ npm run bharat
 */

function sum (a, b) {
    return a + b;
}

const ans = sum(1, 2);

console.log(ans);